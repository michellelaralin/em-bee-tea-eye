[<% @questions.each do |question| %>{"id":"<%= question.id %>","title":"<%= question.title.html_safe %>","type":"<%= question.type %>","created_at":"<%= question.created_at %>","updated_at":"<%= question.updated_at %>"<% if question.answers.any? %>,<% question.answers.each do |answer| %>"answer_<%unless answer.weight == -1%><%= answer.weight %><% else %>neg1<%end%>":"<%= answer.title.html_safe %>"<%= "," unless answer == question.answers.last %><%end%><%end%>}<%= "," unless question == @questions.last %><% end %>]